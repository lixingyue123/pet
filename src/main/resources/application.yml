spring:
  datasource:
    name: petstore
    url: jdbc:mysql://localhost:3306/petstore?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublickeyRetrieval=true&serverTimezone=UTC
    username: root
    password: lxy790524
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.springbootdemo.bean